const numbers = [1,2,3];

const doubleNumArray = numbers.map((num) => {
    return num * 2;
})

console.log(numbers);
console.log(doubleNumArray);

// This function will create new Array and the operation will apply on each and every element of the array.